This update modifies the `receivedInvites.forEach` loop to discriminate between `pendingInviteFeesIncluded` and `pendingCancelledInviteFeesIncluded` when calculating outstanding fees, with a fallback for older records.
```